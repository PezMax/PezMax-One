PezMax-Desktop/
target/
dist/
node_modules/
.qoder/
**/*.rs.bk
Cargo.lock

# build 产物（不提交编译结果和 pdfium 二进制，但提交构建脚本）
build/dist/
build/rust-target/

# PDFium 预编译库（各平台自动下载，不提交）
build/pdfium/**/*.so
build/pdfium/**/*.dylib
build/pdfium/**/*.dll
build/pdfium/**/*.lib

# 敏感凭证（本地登录 token/加密密码等）
credentials.json
**/credentials.json
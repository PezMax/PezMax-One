开发需先执行 `npm install`（postinstall 会运行 `electron-builder install-app-deps` 安装原生依赖）；启动命令区分模式：`npm run dev` 或 `npm run dev:client` 以 client 模式启动，`npm run dev:admin` 以 admin 模式启动（通过环境变量 `VITE_AUTH_ENTRY_MODE` 切换）；打包命令 `build:win|mac|linux` 会先 clean 再 build 并调用 electron-builder 生成对应平台安装包。